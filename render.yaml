services:
  - type: web
    name: data-cleaner-api
    runtime: python3
    buildCommand: pip install -r requirements-ultra.txt
    startCommand: python -m uvicorn main_simple:app --host 0.0.0.0 --port $PORT
    plan: free
